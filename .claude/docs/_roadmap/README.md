# KMPilot Roadmap

Trackable execution plan for the 2026-H2 packaging work. One file per phase, one branch per phase, one PR per phase.

**This directory is upstream-only.** `install.sh` runs `rm -rf .claude/docs` (install.sh:475) and `_roadmap` appears in no `update.sh` tier, so nothing here reaches a downstream project.

---

## Status

| Phase | File | Branch | Status |
|---|---|---|---|
| — | Scaffold (this directory) | `roadmap-scaffold` → merged locally into `main-github` | 🔵 In progress |
| 0 | [Quick wins](PHASE-0-quick-wins.md) | `phase-0-quick-wins` | ⬜ Not started |
| 1 | [Deterministic arch checker](PHASE-1-arch-checker.md) | `phase-1-arch-checker` | ✅ Done ([#2](https://github.com/ThisIsSadeghi/KMPilot/pull/2)) |
| 2 | [Adopt into an existing project](PHASE-2-adopt-mode.md) | `phase-2-adopt-mode` | ✅ Done ([#3](https://github.com/ThisIsSadeghi/KMPilot/pull/3)) |
| 3 | [Plugin packaging](PHASE-3-plugin-packaging.md) | `phase-3-plugin-packaging` | ⏸ Built, parked — publishes after `migrate-feature` |
| 4 | [HTML design source](PHASE-4-html-design-source.md) | `phase-4-html-design-source` | ⬜ Not started |
| — | [Parked](PARKED.md) | — | ⏸ Deliberately unscheduled |
| — | [Adopt test backlog](ADOPT-TEST-BACKLOG.md) | — | 🔁 Living — project shapes still untried |

Legend: ⬜ Not started · 🔵 In progress · 🟡 In review (PR open) · ✅ Done · ⏸ Parked

**This table is the single source of truth for phase status.** Individual phase files describe *what* and *how*, never *where we are*.

---

## Running a phase in a fresh session

Each phase file is self-contained. Point Claude at exactly one:

```
read .claude/docs/_roadmap/PHASE-1-arch-checker.md and start it
```

**What loads for free in every session** (no need to paste or ask for it): `CLAUDE.md` (project instructions), `patterns.md` via its `@`-import (all 14 rules, naming, feature structure), and the memory index — which records the locked decisions and points back here.

**What Claude cannot supply — check the phase file's *Prerequisites* block before starting:**

| Phase | Human-supplied input needed |
|---|---|
| 0 | The two Medium **friend-link URLs** |
| 1 | *(none)* |
| 2 | A **scratch KMP target repo** outside KMPilot, and its path |
| 3 | *(none — CLI support verified)* |
| 4 | A **Figma dev-mode HTML export** fixture, and its path |

**One file at a time.** Reading all seven costs ~1000 lines of context for phases you are not working on. This `README.md` alone is enough to see where things stand.

---

## Why this roadmap exists

Measured 2026-07-30: 42 stars, **23 lifetime `install.sh` downloads**, ~110 unique visitors per fortnight, and a 23% unique-visitor→cloner rate. The landing page converts fine — the funnel is starved, and the one conversion event on offer requires starting a brand-new app.

GitHub path data says what visitors actually want:

| Path | Views / uniques (14d) |
|---|---|
| landing page | 219 / 96 |
| `/tree/main/.claude` | 14 / **9** |
| `/tree/main/.claude/agents` | 8 / 5 |
| `/tree/main/.claude/skills` | 6 / **6** |
| `/blob/…/commands/review-feature.md` | 6 / 4 |

Roughly 1 in 6 landing visitors opens `.claude/` to read the pipeline. None of them can run any of it without cloning a template and starting over. Closing that gap is the whole roadmap.

**Repositioning:** *a template for new KMP apps* → **the KMP adoption pipeline**, with three doors instead of one.

| Door | Entry point | Audience | Relative pool |
|---|---|---|---|
| Start | `install.sh` (exists) | Greenfield KMP | 1× |
| **Adopt** | Phase 2 | Existing KMP app | ~10× |
| Migrate | Parked (`migrate-feature`) | Android app wanting iOS | ~10–50× |

---

## Locked decisions (2026-07-30)

| Decision | Choice | Why |
|---|---|---|
| Adopt-mode model | **Vendor** `core/*` into the target repo | Ships in ~2 weeks and proves demand. Capability-map (enforce rules against *their* `Result`/design system/DI) is the destination but is speculative before any adopter exists |
| Skill naming | **Ship both surfaces.** Template keeps `.claude/` short names; plugin ships `/kmpilot:*` | Two published articles, a 25-minute video, and the wiki all show `/create-feature`. The video cannot be re-cut |
| Authored source | **Plugin tree**, with the template's `.claude/` generated by `scripts/release.sh` | One place to edit, two published surfaces, no drift |
| Design input | **HTML sources only** (Stitch MCP / Figma export / hand-written). Screenshots rejected | `/verify-ui` audits code ↔ HTML. HTML guessed from a PNG makes that audit circular and falsifies the "verified against the design" claim |
| Scope | **Stay KMP-only.** Generalize in the writing, never in the code | The name, the niche, and both converting audience pools are Kotlin/KMP and Claude Code |
| Checker delivery | Python script (`.claude/skills/_shared/`) + thin Gradle `Exec` task | `.claude/skills` is an `update.sh` **OVERRIDE** path → auto-delivers to existing installs with zero conflicts |
| Checker strictness | **Two tiers, one rule set.** A KMPilot project — this repo included — is held to every rule as an `error`. `--baseline` (same checks, errors reported as warnings, exit 0) is the pre-adoption tier | Decided 2026-07-31 while landing Phase 1. Leniency belongs to codebases that have not migrated yet, never to the reference implementation — a template that exempts itself from a rule cannot enforce it. Pre-existing violations get fixed, not downgraded. `--baseline` is also what Phase 2's compatibility report consumes |
| Phase landing | Branch → PR → merge, one phase at a time | CI gate before merge, public review trail, each phase independently revertable |
| Bitbucket mirror | **Ignored** for the whole roadmap | `origin-bitbucket/main` is behind at `edea204`; re-syncing is a separate decision |

---

## Branch and PR conventions

**Local-first.** A phase is developed entirely on a local branch. Nothing is pushed until its exit criteria are all ticked — `origin` only ever sees finished phases.

```bash
# start a phase
git switch main-github && git pull origin-github main
git switch -c phase-N-<slug>
# … work; commit locally as often as useful; do NOT push …

# phase finished (every exit-criteria box ticked)
git push -u origin-github phase-N-<slug>
gh pr create --repo ThisIsSadeghi/KMPilot --base main --head phase-N-<slug>
# merge on GitHub, then resync:
git switch main-github && git pull origin-github main
```

- **Do not push mid-phase.** No WIP branches on origin. If a phase is abandoned, the branch is deleted locally and nothing was ever published.
- **Two remotes exist.** `origin-github` (`ThisIsSadeghi/KMPilot`, canonical) and `origin-bitbucket` (`nalacard/kmpilot`, mirror, deliberately untouched). Always name the remote explicitly — a bare `git push` is ambiguous here.
- There is **no local branch named `main`**. `main-github` tracks `origin-github/main`.
- **Claude never commits or pushes.** Changes are prepared and diffed; every `git commit` / `git push` / merge is driven by hand.
- A phase's PR merges before the next branch is cut, so the status table above always reflects reality.
- Local `main-github` may sit ahead of `origin-github/main` between phases (e.g. this scaffold was merged locally). That is expected on a solo repo and fast-forwards on the next push.

---

## `update.sh` delivery tiers — consult before adding files

Where a changed path sits decides whether existing downstream installs get it automatically.

| Tier | Paths | Behaviour on `./update.sh` |
|---|---|---|
| **OVERRIDE** | `.claude/skills`, `.claude/agents`, `.claude/commands`, `.claude/hooks` | Release version applied as-is. No conflicts. **Best home for new tooling.** |
| **TIER1 (merge)** | `.claude/settings.json`, `.claude/docs/_shared`, `CLAUDE.md`, `gradlew`, `gradlew.bat`, `gradle/wrapper`, `update.sh` | 3-way merged |
| **TIER2** | `core` | Rename-aware merge, only with `--core` |
| **MANUAL** | `gradle/libs.versions.toml`, `build.gradle.kts`, `settings.gradle.kts`, `composeApp`, `androidApp`, `iosApp` | Surfaced, never auto-merged. Needs a `[Manual]` CHANGELOG entry |
| *(untiered)* | everything else, incl. `.github`, `assets`, `scripts`, `.claude/docs/_roadmap` | Not delivered downstream at all |

Source: `update.sh:170-176`. Stripped-on-install paths: `install.sh:475-489`, `753`.

---

## Phase file template

Every `PHASE-*.md` uses the same headings so they stay comparable:

**Goal · Why now · Branch · In scope · Out of scope · Files touched · Steps · Exit criteria · Verification · Risks · Downstream delivery**
